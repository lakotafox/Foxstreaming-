// Preload script - runs before web content loads
// Currently minimal, but can expose APIs to renderer if needed

window.addEventListener('DOMContentLoaded', () => {
  console.log('FoxStream loaded');
});
